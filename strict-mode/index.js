/**
 * use strict forces the user to follow some rules in JS so as not to generate errors
 */
"use strict"; //ativate strict mode

//no strict mode
nome="Bruno";
console.log(nome);

//strict mode
let nome="Bruno";
console.log(nome);